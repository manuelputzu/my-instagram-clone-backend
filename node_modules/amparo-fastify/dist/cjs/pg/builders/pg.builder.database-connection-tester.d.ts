import { Pool } from "pg";
declare const databaseConnectionTesterBuilder: (pool: Pool, variant: string, variantMessages: Record<string, {
    success: string;
    failure: string;
}>) => () => Promise<{
    success: boolean;
    error: null;
    message: string;
} | {
    success: boolean;
    error: Object;
    message: string;
}>;
export { databaseConnectionTesterBuilder };
//# sourceMappingURL=pg.builder.database-connection-tester.d.ts.map
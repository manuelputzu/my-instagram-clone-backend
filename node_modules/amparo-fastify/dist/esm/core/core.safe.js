import getErrorConstructor from "../common/helpers/helpers.get-error-constructor";
async function safe(value, argument2, argument3) {
    try {
        const resolvedValue = await value;
        return resolvedValue;
    }
    catch (error) {
        if (typeof argument2 === "function") {
            const [errorMessage, errorName] = argument2(error);
            throw new (getErrorConstructor(errorName))(errorMessage, error);
        }
        else if (typeof argument2 === "string" && argument3) {
            throw new (getErrorConstructor(argument3))(argument2, error);
        }
    }
}
export { safe };
//# sourceMappingURL=core.safe.js.map
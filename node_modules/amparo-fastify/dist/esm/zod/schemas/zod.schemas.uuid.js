import z from "zod";
const uuidSchema = z.string().uuid().nonempty();
const isUuid = (value) => uuidSchema.safeParse(value).success;
export { uuidSchema, isUuid };
//# sourceMappingURL=zod.schemas.uuid.js.map